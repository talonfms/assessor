<div class="nested-fields" data-new-record="<%= f.object.new_record? %>">
  <div class="form-group">
    <%= f.label :value %>
    <%= f.text_field :value, class: 'form-control' %>
  </div>
  <button type="button" data-action="click->block-options#removeOption" class="btn btn-danger btn-sm">Remove Option</button>
</div>